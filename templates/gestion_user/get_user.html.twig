{% extends 'base.html.twig' %}

{% block title %}Informations Utilisateur{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <h1 class="text-center mb-4">Informations Utilisateur</h1>
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Informations Utilisateur</h5>
                        <p class="card-text"><strong>Prénom:</strong> {{ user.firstname }}</p>
                        <p class="card-text"><strong>Nom:</strong> {{ user.lastname }}</p>
                        <p class="card-text"><strong>Email:</strong> {{ user.email }}</p>
                        <p class="card-text"><strong>Genre:</strong> {{ user.genre }}</p>
                        <!-- Ajoutez les autres champs de votre utilisateur ici -->
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
